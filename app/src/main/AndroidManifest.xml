<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Eliminado MANAGE_EXTERNAL_STORAGE -->
    <!-- Permite consultar apps instaladas específicas (Android 11+) -->
    <queries>
        <package android:name="com.just6889port.ddlc" />
        <package android:name="com.z.mas.portby.just6889" />
        <package android:name="com.test.ddbs" />
        <package android:name="com.dimensions.port" />
        <package android:name="com.z.dd.exitmusic.portby.just6889" />
        <package android:name="com.test.dokibanzai" />
        <package android:name="com.test.ddlcnight" />
        <package android:name="com.test.ourtime" />
        <package android:name="com.test.flc" />
        <package android:name="com.z.ddreflection.portby.just6889.tdclub" />
        <package android:name="com.just6889port.crimsomdevotion" />
        <package android:name="com.z.our.bad.ending.portby.just6889" />
        <package android:name="com.just6889.ddmidnight" />
        <package android:name="com.just6889.theblueseas" />
        <package android:name="com.just6889.nightmareyanderedoki" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TraduLauncher">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <receiver
            android:name=".data.DownloadCompleteReceiver"
            android:exported="false" />
        <receiver
            android:name=".data.UninstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TraduLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>